<form method="post" action="{{ path('app_logins_delete', {'id': login.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ login.id) }}">
    <button class="btn">Delete</button>
</form>
