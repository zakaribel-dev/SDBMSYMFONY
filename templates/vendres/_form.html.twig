
 <br>
 <a class="new" href="{{ path('app_vendres_index', {'numeroTicket': numeroTicket}) }}">Retour</a> <br> <br>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <br>
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
