{% extends 'base.html.twig' %}

{% block title %}Ticket index{% endblock %}

{% block body %}
    <h1>Ticket index</h1>

<button> 
<a href="{{ path('app_tickets_new') }}">NOUVELLE LIGNE</a>
</button> 
<br>
<br>
    <table class="table table-striped table-dark">
        <thead>
            <tr>
                <th>Annee</th>
                <th>NumeroTicket</th>
                <th>DateVente</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ticket in tickets %}
            <tr>
                <td>{{ ticket.annee }}</td>
                <td>{{ ticket.numeroTicket }}</td>
                <td>{{ ticket.dateVente ? ticket.dateVente|date('Y-m-d H:i:s') : '' }}</td>
                <td>
               <a href="{{ path('app_tickets_show', {'annee': ticket.annee}) }}">show</a>
<a href="{{ path('app_tickets_edit', {'numeroTicket': ticket.numeroTicket}) }}">edit</a>
                <a href="{{ path('app_vendres_index', {'numeroTicket': ticket.numeroTicket}) }}">Voir détails</a>

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_tickets_new') }}">Create new</a>
{% endblock %}
